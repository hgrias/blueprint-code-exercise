export const db = {
  screener: {
    findUnique: jest.fn(),
  },
  assessment: {
    findMany: jest.fn(),
  },
  domain: {
    findMany: jest.fn(),
  },
};
export const db = {
  screener: {
    findUnique: jest.fn(),
  },
  assessment: {
    findMany: jest.fn(),
  },
  domain: {
    findMany: jest.fn(),
  },
};
